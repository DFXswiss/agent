{
  "schema": "pr-guard/v1",
  "a38": {
    "enforce": ["integration"],
    "exclude": ["release"],
    "default": "enforce"
  },
  "environment_approval": {
    "enabled": true,
    "environment": "pr-ci",
    "workflows": [".github/workflows/pr.yml"]
  }
}
